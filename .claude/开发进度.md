# 天衢 Skill + MCP 开发进度

## 当前状态
> 最后更新：2026-05-03
> 已完成 MCP Server 四工具实现、Skill 工作流、一键安装脚本、版本治理体系。
> kmp 项目中所有 17 项天衢能力的验证代码已编写完成，编译 0 错误 0 警告。
> install-claude.sh 提示语已修正（补 `/tianqu` 前缀）；CheckUpdateTool 返回 JSON 已改为正式序列化构造；新 JAR 已重新编译并同步到 bin/。
> **install-claude.sh 与 tianqu_check_update 工具仅在 kmp 项目（天衢自身 MCP 注册环境）完成了代码逻辑验证，尚未在一个干净的第三方项目中做完整的端到端安装流程验证。**
> 下一步：找一个全新的空白 KMP 项目，从零执行 `curl ... | bash` 安装脚本，完整走一遍「安装 → 启动 → /tianqu 接入框架 → 检查升级」流程。

## 步骤进度

- [x] Step 1. 确认架构方案（Skill + 本地 MCP）
- [x] Step 2. 创建计划文档（`.claude/天衢接入计划.md`）与进度追踪文档（本文件）
- [x] Step 3. 新建 `tianqu-mcp-server` Gradle 模块，注册到 `settings.gradle.kts`
- [x] Step 4. 实现 `GetCapabilitiesTool.kt` + `capabilities.json`（升级为带 meta 的对象结构）
- [x] Step 5. 实现 `AnalyzeProjectTool.kt`
- [x] Step 6. 实现 `GetIntegrationGuideTool.kt`（接入指南已资源化为 `integration-guides/app.md` + `feature.md`）
- [x] Step 7. 编写 `Main.kt`，注册四个 Tool，启动 `StdioServerTransport`
- [x] Step 8. 创建/更新 `.claude/skills/tianqu/SKILL.md`（增加四步工作流、force 更新规则）
- [x] Step 9. 更新 `.claude/settings.json` 注册 `tianqu` MCP（路径改为相对路径）
- [x] Step 10. 补充接入方使用说明（README.md 补充 Claude Code 接入章节）
- [x] Step 11. 端到端验证——kmp 项目（正向 + 反向用例）
  - [x] 测试环境已准备（kmp 项目已拷贝 JAR + Skill + settings）
  - [x] `/mcp` 确认可见 `tianqu` 四个工具
  - [x] 正向用例：「帮我接入天衢框架」—— kmp 项目已成功接入，验证通过
  - [x] 反向用例：「用天衢做一个 Fragment 的路由页面」（已补充负向示例文件说明）
- [x] Step 12. kmp 项目天衢全功能验证（17 项全覆盖，编译 BUILD SUCCESSFUL）
  - [x] 一、引入与配置（子模块 + 主工程 Gradle 配置）
  - [x] 二-1. 全局初始化 RouterHost + rememberNavigator + Guards
  - [x] 二-2. 获取 Navigator 的多种方式（LocalNavigator / coroutineContext）
  - [x] 二-3. 物理返回键/侧滑手势返回拦截（BackHandler expect/actual）
  - [x] 二-4. 路由启动模式：SINGLE_TOP / SINGLE_TASK
  - [x] 二-5. 自定义页面切换动画（@Transition + BaseTransitionStrategy）
  - [x] 三-1. URL 传参：Path 变量 + Query 参数
  - [x] 三-2. 复杂大对象传参（extra）
  - [x] 三-3. 强类型安全传参（navigateArgs + getTypedArgs）
  - [x] 四. 非阻塞并发数据预加载（RoutePreloader + rememberPreloadData）
  - [x] 五-1. 页面结果回传：挂起式 awaitNavigateForResult
  - [x] 五-2. 页面结果回传：回调式 navigateWithResult
  - [x] 六. 跨模块服务发现（@Service + ServiceManager.getService）
  - [x] 七-1/2/3. ViewModel：反射无参 / 自定义 Factory / @InjectViewModel KSP 自动注入
  - [x] 八. 动态按需懒加载（RouterGuard + registerDynamicRoutes）
  - [x] 九-1. 多返回栈嵌套 + Tab 状态持久化（SaveableStateHolder）
  - [x] 九-2. Compose 共享元素动画（routerSharedBounds）
  - [x] 九-3. 全局 404 降级拦截（RouterEvent.NotFound）
  - [x] 九-4. 离线 DeepLink 意图缓存（DeepLinkManager）
  - [x] 附加. 页面状态恢复（remember vs rememberSaveable）
- [x] Step 13. 版本治理体系
  - [x] 建立 `version.json`（仓库根目录）+ `tianqu-mcp-server/src/main/resources/tianqu-version.json`，双文件保持一致
  - [x] `capabilities.json` 升级为带 `meta` 的对象结构（含 `capabilitiesVersion`、`frameworkVersion`、`lastUpdated`）
  - [x] Integration Guide 资源化（`integration-guides/app.md` + `feature.md`，含占位符替换）
  - [x] 新增 `tianqu_check_update` 工具（24h 本地缓存 + 远端 Gitee Raw 比对 + `force` 强制刷新参数）
  - [x] SKILL.md 更新为四步工作流（check_update 排在第 1 步）
  - [x] `install-claude.sh` 一键安装脚本（下载 JAR + Skill + 注册 MCP）
  - [x] README.md 补充 Claude Code 接入章节与强制更新说明
  - [x] CLAUDE.md 补充发版检查清单
  - [x] 重新编译 JAR，复制到 `bin/tianqu-mcp-server-all.jar`
- [ ] Step 14. install-claude.sh + tianqu_check_update 端到端验证（⚠️ 尚未完成）
  - [ ] 在一个全新的空白 KMP 项目中执行 `curl -fsSL .../install-claude.sh | bash`
  - [ ] 验证 JAR 下载、Skill 写入、MCP 注册三步均成功
  - [ ] 在该项目启动 Claude Code，确认 `/mcp` 可见 `tianqu` 四个工具
  - [ ] 执行 `/tianqu 帮我接入天衢框架`，走完完整 Skill 流程（含 `tianqu_check_update`）
  - [ ] 验证 `tianqu_check_update` 首次调用写入缓存、第二次命中缓存、`force=true` 强制刷新

### 已修复问题
- `install-claude.sh` 脚本的安装完成提示文案，补全了触发指令的 `/tianqu` 前缀，解决可见性问题。
- `CheckUpdateTool.kt` 的返回结果 `buildResultJson` 从易碎的纯手拼字符串改为使用 `kotlinx.serialization` 的原生 JSON 构造，提高稳定性和序列化规范。
- `.claude/settings.json` 中 MCP JAR 路径由绝对路径改为相对路径（`tianqu-mcp-server/build/libs/tianqu-mcp-server-all.jar`），确保跨机器可用
- 端到端验证中发现：Claude Code 可能优先使用本机 `~/.claude.json` 中缓存的项目 MCP 配置，导致项目内 `.claude/settings.json` 已配置但 `/mcp` 仍不显示；已补充排查说明
- `ManualViewModelFactory.create()` 的 `as T` 强转添加了 `@Suppress(“UNCHECKED_CAST”)`，消除编译警告

---

## 待确认事项
（执行过程中遇到的不确定事项记录在此，等用户确认后继续）

- 无

---

## 备注
- 下次继续工作时，请先读取本文档确认当前进度，再从第一个未完成步骤继续
- 当前唯一未验证的是 Step 14：install-claude.sh 的真实安装流程；需要在一个**没有天衢**的干净项目里跑一遍
- 遇到冲突或不确定的地方，不要自行决定，记录到”待确认事项”并告知用户
